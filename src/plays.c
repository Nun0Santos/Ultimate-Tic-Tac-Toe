#include "plays.h"
#include "globals.h"



/*void show(pdata p){
    //Shows contents of connected list
    int count=1;
    printf("printing\n");
    while (p!=NULL){
        printf("struct%d: \nl=%d, c=%d, playtype=%d\n",count++,(*p).l,(*p).c,(*p).playtype);
        p = p -> prox;
    }
}*/